"""Central node-category constants.

Keep all `CATEGORY = ...` strings here so the menu hierarchy stays
consistent as the toolkit grows.
"""

ROOT = "deepsoch"

PROMPT = f"{ROOT}/prompt"
IMAGE = f"{ROOT}/image"
SAMPLER = f"{ROOT}/sampler"
UTIL = f"{ROOT}/util"
